# RAG Student Support System - Environment Configuration
# 
# This file is OPTIONAL. The application works with defaults from config.py.
# Copy this file to .env and uncomment/modify values to override defaults.
#
# Note: .env files are typically gitignored for security. Never commit secrets!

# ============================================================================
# MODE CONFIGURATION
# ============================================================================
# MODE=local  # Options: "local" or "azure"
# LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR

# ============================================================================
# LOCAL MODE SETTINGS (Default - works out of the box)
# ============================================================================
# VECTOR_DB_PATH=./chroma_db
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# Local LLM Settings (for local mode)
# LOCAL_LLM_PROVIDER=ollama  # Options: "ollama" or "transformers"
# LOCAL_LLM_MODEL=tinyllama  # Very small model (1.1B params) - perfect for RAG
# LOCAL_LLM_BASE_URL=http://localhost:11434  # Ollama API URL
# LOCAL_LLM_USE_GPU=false  # Use GPU for transformers (if available)

# Chunking Strategy
# CHUNKING_STRATEGY=sentence  # Options: sentence, semantic, section, recursive
# CHUNK_SIZE=500
# CHUNK_OVERLAP=50

# Conversation Memory
# ENABLE_CONVERSATION_MEMORY=true
# MAX_CONVERSATION_HISTORY=10
# CONVERSATION_TTL_HOURS=24

# ============================================================================
# AZURE MODE SETTINGS (Only needed for Azure deployment)
# ============================================================================
# MODE=azure
# AZURE_SEARCH_ENDPOINT=https://your-service.search.windows.net
# AZURE_SEARCH_KEY=your-azure-search-key
# AZURE_SEARCH_INDEX_NAME=student-knowledge-base
# AZURE_OPENAI_ENDPOINT=https://your-service.openai.azure.com
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================================================
# API SECURITY (Optional)
# ============================================================================
# API_KEY=your-secret-api-key  # Set this to enable API key authentication

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# MAX_RESPONSE_LENGTH=1000
# MIN_CONFIDENCE_SCORE=0.7
# ENABLE_MULTILINGUAL=true

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# HOST=0.0.0.0
# PORT=8000
