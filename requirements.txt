# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
python-multipart==0.0.6

# Azure services (for Azure mode)
azure-search-documents==11.4.0
azure-identity==1.15.0
semantic-kernel>=1.30.0,<2.0.0

# OpenAI SDK
# Note: semantic-kernel 1.30+ requires openai>=1.12.0
# Making version flexible to resolve with semantic-kernel
openai>=1.12.0,<2.0.0

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Language detection
langdetect==1.0.9

# HTTP client
httpx==0.25.2
tenacity==8.2.3

# Local vector database and embeddings
chromadb==0.4.22
sentence-transformers==2.2.2
huggingface-hub<0.20.0

# PDF processing
pypdf==3.17.4
pdfplumber==0.10.3

# Text processing and ML dependencies
# Note: numpy and torch versions are compatible with Python 3.11
# and work with sentence-transformers and chromadb
numpy>=1.24.0,<2.0.0
torch>=2.0.0,<3.0.0
